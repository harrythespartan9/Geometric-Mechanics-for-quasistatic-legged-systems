function [ampArr, phaseArr, biasArr] = generateInputSpaceV2(params)
    %GENERATEINPUTSPACEV2 generate an input space for the purely sinusoidal
    %limb fore-aft angular gait definitions for the CLARI system
    %   For this we need specific information about the system, like the
    %   limits on the fore-aft limb angles, and other system properties to
    %   compute the gait parameter limits and the corresponding
    %   system performance.

    % unpack necessary parameters
    

end